name: lab02_193193
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  cupertino_icons: ^1.0.8
  http: ^1.2.2
  firebase_core: ^3.9.0
  firebase_messaging: 15.1.6  # Added Firebase Messaging for push notifications
  flutter_local_notifications: ^13.0.0 # Added local notifications
  firebase_auth: ^5.3.4
  timezone: ^0.9.0 # Added timezone package for scheduling notifications

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
